
<div class="jumbotron">
  <h1 class="display-4">Task Lists</h1>
  <p class="lead">Your Tasks</p>
  <hr class="my-4">

  <ul>
    <% @tasks.each do |task| %>
      <li>
        <input type="checkbox" <%= task.completed ? "checked" : "unchecked" %>><%= link_to task.title, task_path(task) %>
       <%= link_to task_path(task), method: :delete, data: { confirm: "Are you sure?" } do %>
         <i class="fas fa-trash-alt"></i>
        <% end %>
      </li>
    <% end %>
  </ul>

  <%= link_to 'Ajoutez une tâche', new_task_path %>
</div>
  <a class="btn btn-primary btn-lg" href="#" role="button">Learn more</a>
